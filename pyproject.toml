[project]
name = "vocabulary-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Felix Elstner", email = "felstner@wvh-online.com" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
]

[project.scripts]
vocabulary-app = "vocabulary_app:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
